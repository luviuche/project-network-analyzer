# Dependencies are declared in pyproject.toml; this file only points at
# it, so the two can never drift apart.
#
#     pip install -r requirements.txt     # same as the line below
#     pip install -e ".[dev]"             # project + test dependencies

-e .[dev]
